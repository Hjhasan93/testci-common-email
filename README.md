# testci-common-email

This project demonstrates Continuous Integration using GitHub Actions.

A Maven-based Java project was configured to automatically build and test using:
mvn clean test

GitHub Actions workflow is located in:
.github/workflows/maven.yml

The CI pipeline runs automatically on every push to the main branch.